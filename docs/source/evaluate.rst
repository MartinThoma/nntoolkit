Evaluate Neural Networks
========================

To evaluate a neural network means to propagate input through the network
and get the output of the network.

.. automodule:: nntoolkit.evaluate
   :members: